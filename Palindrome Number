a = eval(input())
flag = 1
for i in range(len(a)//2):
    if(a[i] == a[len(a)-1-i]):
        continue
    else:
        flag=0
        break
if(flag==1):
   print("palindrome")
else:
   print("not palindrome")
    
